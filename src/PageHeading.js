const PageHeading = (props) => {
    return(
        <header>
            {/* props stuff here (maybe some components? some jsx? somethign to display! ) */}
            {props.children}
            <p> This Will Appeare where you see meeee!!</p>
        </header>
    )

}
export default PageHeading;